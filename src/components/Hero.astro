---
// Hero component - improved with better visual hierarchy and engagement
---

<section
  aria-labelledby="hero-title"
  class="relative bg-white overflow-hidden h-[100vh] flex items-start pt-16 md:pt-24"
  style="padding-top: calc(env(safe-area-inset-top, 0px) + 4rem);"
>
  <!-- TEMP: hairline to debug hero height (remove after final layout review) -->
  <!-- Enhanced gradient background -->
  <div
    aria-hidden="true"
    class="absolute inset-0 bg-gradient-to-b  from-slate-50 to-white"
  >
  </div>

  <!-- Subtle noise texture overlay -->
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-0 opacity-[0.025] sm:opacity-5"
    style="
      background-image: url('data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAEAAAAAQCAYAAABQ3a3PAAAACXBIWXMAAAsSAAALEgHS3X78AAAAJ0lEQVRYhe3OsQkAMAwEwZf//2bKBmQnYw5xgQxw+0o8JqQm9i6g3cC2mQmY0m8G0VjH2wAAAABJRU5ErkJggg==');
      background-size: 200px 200px;
      background-repeat: repeat;
      mask-image: radial-gradient(circle at 50% 50%, black 50%, transparent 100%);
      -webkit-mask-image: radial-gradient(circle at 50% 50%, black 50%, transparent 100%);
      image-rendering: pixelated;
    "
  >
  </div>

  <div class="relative mx-auto pt-24 max-w-[46rem] px-5 sm:px-6 text-center">
    <!-- Pre-headline -->
    <div class="mb-8 animate-fade-in-up">
      <p
        class="text-xs font-medium tracking-widest text-[#D4AF37] uppercase mb-2"
      >
        Life Coaching Excellence
      </p>
      <div class="w-12 h-0.5 bg-[#D4AF37] mx-auto"></div>
    </div>

    <!-- Main headline with better typography -->
    <h1
      id="hero-title"
      class="mb-4 sm:mb-6 text-[clamp(2.25rem,7vw,4.25rem)] md:text-[clamp(3rem,5vw,5rem)] font-extrabold leading-[0.95] tracking-tight text-ink animate-fade-in-up [animation-delay:0.2s]"
    >
      <span class="font-black">Transform.</span> Succeed.
    </h1>

    <!-- Enhanced subheadline -->
    <div
      class="mx-auto max-w-2xl mb-8 sm:mb-12 animate-fade-in-up [animation-delay:0.4s]"
    >
      <p class="leading-relaxed text-gray-600">
        Breakthrough, not just advice.
      </p>
    </div>

    <!-- Improved CTA section -->
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-3 sm:gap-4 mb-8 sm:mb-16 animate-fade-in-up [animation-delay:0.6s]"
    >
      <a
        href="#book-call"
        class="group w-full sm:w-auto inline-flex items-center justify-center rounded-full bg-black text-white px-8 py-4 text-base font-semibold shadow-lg ring-1 ring-[#D4AF37]/40 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-black/20 hover:ring-2 hover:ring-[#D4AF37]/60 focus:outline-none focus-visible:ring-2 focus-visible:ring-[#D4AF37]/60 active:translate-y-px"
        aria-label="Book your free consultation call"
      >
        Book Your Free Call
        <svg
          class="ml-2 w-5 h-5 shrink-0 transition-transform duration-300 group-hover:translate-x-1"
          viewBox="0 0 24 24"
          fill="none"
          aria-hidden="true"
        >
          <path
            d="M5 12h14M13 5l7 7-7 7"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </a>

      <a
        href="/services"
        class="group w-full sm:w-auto inline-flex items-center justify-center rounded-full bg-white text-ink px-8 py-4 text-base font-semibold shadow-sm ring-1 ring-gray-300 hover:ring-2 hover:ring-[#D4AF37]/50 transition-all duration-300 hover:-translate-y-0.5 hover:shadow-md focus:outline-none focus-visible:ring-2 focus-visible:ring-[#D4AF37]/60 active:translate-y-px"
      >
        View Our Services
        <svg
          class="ml-2 w-5 h-5 shrink-0 transition-transform duration-300 group-hover:translate-x-0.5"
          viewBox="0 0 24 24"
          fill="none"
          aria-hidden="true"
        >
          <path
            d="M5 12h14M13 5l7 7-7 7"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </a>
    </div>

    <!-- Social proof / trust indicators -->
    <div
      class="flex flex-wrap items-center justify-center gap-x-4 gap-y-2 text-[13px] sm:text-sm text-gray-500 animate-fade-in-up [animation-delay:0.8s]"
    >
      <div class="flex items-center gap-2">
        <span>Certified Life Coach</span>
      </div>

      <div class="hidden sm:block w-1 h-1 rounded-full bg-gray-300"></div>

      <div class="flex items-center gap-2">
        <span>100+ Lives Transformed</span>
      </div>

      <div class="hidden sm:block w-1 h-1 rounded-full bg-gray-300"></div>

      <div class="flex items-center gap-2">
        <span>Personalized Approach</span>
      </div>
    </div>

    <!-- Scroll indicator -->
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0;
  }
</style>
